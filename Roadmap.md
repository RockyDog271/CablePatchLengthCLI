# ROADMAP  
## Current Version is V0.3  
  
## Planned changes for V0.4+  
> Ability to loop the project from Main, perhaps a menu  
> Changing of the dictionaries used to classes  
> Moving the options (syntax, debug, values, magic numbers, etc) to a centralized file  
> change Hardcoded ANSI to colorama  
> Wildcards are used for imports, Removal of that  
  
## Planned changes for V0.5+  
> Explore/enable a better way to enable implement scripting  
> Add Black/Flake/Pylint to the requirement.dev file  
> Automated Script support  
> Add docstrings and type hints  
  
## Planned changes for V0.6+  
> Add CLI flags (argparse/click)  
> Add input checks to not allow negative values  
> Add checks for (NaN/inf)  
> data_input_script should return a dataclass or a dict  
> .editorconfig? suggested...  
  
## Planned changes for V0.7+  
> add requirements.txt if still missing  
> add pyproject.toml and packaging  
> adding of CI github actions to run the test suite on push  
  
## Planned changes for V0.8+  
> Complete, improve rewording of all prompts

## Planned changes for V0.9+  
> Better support for selecting type of hardware, less reliance on magic numbers
> Improvement of program scalability

## Planned changes for V1.0+  
A brief note, I do not foresee this making it to Version 1 without a collaborator  
There is a point where the learning from a program only I use is not work the continued time investment  
> A full menu when program starts  
